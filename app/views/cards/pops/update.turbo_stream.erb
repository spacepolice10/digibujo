<%= turbo_stream.remove @card %>
<% if @card.popped? %>
  <%= turbo_stream.prepend "popped_cards" do %>
    <%= render partial: "cards/card", locals: { card: @card } %>
  <% end %>
<% else %>
  <%= turbo_stream.append "cards" do %>
    <%= render partial: "cards/card", locals: { card: @card } %>
  <% end %>
<% end %>
