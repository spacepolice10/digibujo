<%= turbo_stream.remove @access_code %>
<% if Current.user.access_codes.none? %>
  <%= turbo_stream.replace "access_codes" do %>
    <p id="access_codes_empty">No access codes yet.</p>
  <% end %>
<% end %>
