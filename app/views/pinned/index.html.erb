<% if request.headers["Turbo-Frame"] == "pinned_list" %>
  <turbo-frame id="pinned_list" class="dropdown pinned--list" popover>
    <%= render "pinned/entities_list",
          pinned_entities: @pinned_entities,
          compact: true,
          popover: true %>
  </turbo-frame>
<% else %>
  <% content_for(:title, "Pinned") %>
  <div class="layout--page pinned--page"<% unless request.variant.include?(:mobile) %> data-controller="bulk-menu"<% end %>>
    <section class="pinned--workspace">
      <%= render "pinned/entities_list",
            pinned_entities: @pinned_entities,
            compact: request.variant.include?(:mobile),
            popover: false %>
    </section>
    <% unless request.variant.include?(:mobile) %>
      <%= render "bullets/bulk_menu" %>
    <% end %>
  </div>
<% end %>
