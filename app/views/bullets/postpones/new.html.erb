<turbo-frame id="postpone_picker_dropdown_id" class="bulk-menu--action-menu bulk-menu--action-frame" popover>
  <%= render "picker",
        bullet_ids: params[:bullet_ids].to_s,
        asap: @asap,
        tomorrow: @tomorrow,
        next_monday: @next_monday,
        next_weekends: @next_weekends,
        daylog_bucket: @daylog_bucket,
        daylog_buckets: @daylog_buckets,
        future: @future,
        monthlylog: @monthlylog %>
</turbo-frame>
