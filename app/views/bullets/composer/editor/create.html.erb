<turbo-frame id="composer_editor">
  <%= fields_for :bullet, @bullet do |f| %>
    <%= render "bullets/composer/editor",
          f: f,
          bullet: @bullet,
          type_name: @type_name,
          editing: false,
          body_html: @body_html %>
  <% end %>
</turbo-frame>
