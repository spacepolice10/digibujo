<% content_for(:title) { "Edit bullet" } %>
<header class="layout--header">
  <%= link_to bullet_path(@bullet), class: "button--tertiary", data: { turbo_frame: "_top" } do %>
    <i class="icon" style="--icon-mask: var(--icon-arrow-left)" aria-hidden="true"></i>
    Back
  <% end %>
  <%= button_to bullet_path(@bullet), method: :delete, class: "button--secondary button--danger", form: { data: { turbo_confirm: "Are you sure?" } } do %>
    <i class="icon" style="--icon-mask: var(--icon-trash)" aria-hidden="true"></i>
    Delete
  <% end %>
</header>
<turbo-frame id="<%= dom_id(@bullet) %>"
             class="bullet"
             data-bullet-type="<%= @bullet.bulletable_type.downcase %>">
  <%= render "bullets/bulletable_form",
        bullet: @bullet,
        attributes: {},
        editing: true %>
</turbo-frame>
