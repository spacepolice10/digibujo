<turbo-frame id="<%= dom_id(@monthly_bucket, @bullet.pops_on) %>">
  <%= render Bullet.composer_partial_of(@bullet.bulletable_type), bullet: @bullet, bullets_path: monthly_bucket_bullets_path(@monthly_bucket) %>
</turbo-frame>
