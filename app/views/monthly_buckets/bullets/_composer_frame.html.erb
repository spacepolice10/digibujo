<%# locals: (monthly_bucket:, pops_on: nil) %>
<turbo-frame id="<%= monthly_bucket_composer_frame_id(pops_on) %>"
             class="<%= monthly_bucket_composer_frame_class(pops_on) %>"
             data-controller="composer-frame">
</turbo-frame>
