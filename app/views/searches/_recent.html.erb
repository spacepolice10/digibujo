<h4 class="search--section-heading">Recent</h4>
<ul class="layout--list menu--palette-list" role="listbox">
  <% selections.each do |selection| %>
    <%= render "searches/palette_item", entity: selection.searchable %>
  <% end %>
</ul>
