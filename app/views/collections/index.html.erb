<% content_for(:title, "Collections") %>

<main class="layout--container" data-size="md" data-padding="true">
  <div class="layout--surface layout--flex" data-elevation="1" data-padding="true" data-direction="column">
    <header class="layout--container-header">
      <%= back_link_to home_path, class: "button", data: { content: "text", turbo_frame: "_top" }, aria: { label: "Back to Home" } do %>
        <%= icon_tag("arrow-left") %><span>Home</span>
      <% end %>
      <h1>Collections</h1>
      <%= link_to new_collection_path, class: "button layout--container-header-action", data: { intent: "primary", radius: "full", size: "xs" } do %>
        <%= icon_tag("plus") %><span>Create</span>
      <% end %>
    </header>

    <div id="collections">
      <% if @collections.any? %>
        <div class="layout--grid collection--list" data-cols="2">
          <%= render partial: "collections/collection", collection: @collections, as: :collection %>
        </div>
      <% else %>
        <p class="typography--paragraph-sm">No collections yet.</p>
      <% end %>
    </div>
  </div>
</main>
