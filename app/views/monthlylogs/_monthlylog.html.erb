<%= link_to monthlylog_path(monthlylog), class: "bucket--section-list-item" do %>
  <%= icon_tag(monthlylog.icon, style: "color: #{colour(monthlylog.colour)};") %>
  <span class="utilities--line-clamp-1"><%= monthlylog.name %></span>
<% end %>
