<% content_for(:title) { "Enter code" } %>

<div class="session--page">
  <div class="form--surface">
    <%= render "authentication/flash" %>

    <%= form_with url: authentication_confirmation_path, class: "form form--strong" do |form| %>
      <div class="session--intro">
        <h1 class="session--title">Check your email</h1>
        <p class="form-hint session--description">We sent a 6-character code to <strong><%= @email %></strong>.</p>
      </div>

      <div class="form-group">
        <%= form.label :code, "Login code", class: "form-label" %>
        <%= form.text_field :code,
              class: "form-input session--code-input",
              required: true,
              autofocus: true,
              autocomplete: "one-time-code",
              inputmode: "text",
              maxlength: 6,
              placeholder: "······",
              aria: { label: "6-character login code" } %>
      </div>

      <%= render "authentication/submit_button", form: form, label: "Verify code" %>
    <% end %>
  </div>

  <%= link_to "Didn't receive it? Try again", new_authentication_path, class: "session--footer-link" %>
</div>
