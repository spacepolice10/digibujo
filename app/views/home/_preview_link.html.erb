<%= link_to path, class: "home--preview-link", data: { turbo_frame: "_top" } do %>
  <%= icon_tag(icon, style: local_assigns[:colour] ? "color: #{colour};" : nil) %>
  <span class="utilities--line-clamp-1"><%= name %></span>
<% end %>
