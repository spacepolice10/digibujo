<div class="dropdown--element-body">
  <nav class="dropdown--menu" aria-label="Create">
    <%= link_to new_collection_path,
          class: "dropdown-item",
          data: {
            turbo_frame: "_top",
            grid_navigation_target: "item",
            action: "focus->grid-navigation#syncPosition"
          } do %>
      <%= icon_tag("plus") %>
      Add collection
    <% end %>

    <%= link_to current_future_path,
          class: "dropdown-item",
          data: {
            turbo_frame: "_top",
            grid_navigation_target: "item",
            action: "focus->grid-navigation#syncPosition"
          } do %>
      <%= icon_tag("calendar") %>
      Future log
    <% end %>

    <%= link_to trackers_path,
          class: "dropdown-item",
          data: {
            turbo_frame: "_top",
            grid_navigation_target: "item",
            action: "focus->grid-navigation#syncPosition"
          } do %>
      <%= icon_tag("target") %>
      Trackers
    <% end %>

    <%= link_to current_monthlylog_navigation_path,
          class: "dropdown-item",
          data: {
            turbo_frame: "_top",
            grid_navigation_target: "item",
            action: "focus->grid-navigation#syncPosition"
          } do %>
      <%= icon_tag("calendar") %>
      Monthly log
    <% end %>

    <%= link_to new_project_path,
          class: "dropdown-item",
          data: {
            turbo_frame: "_top",
            grid_navigation_target: "item",
            action: "focus->grid-navigation#syncPosition"
          } do %>
      <%= icon_tag("tag") %>
      New project
    <% end %>
  </nav>
</div>
