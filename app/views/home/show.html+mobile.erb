<% content_for(:title, "Home") %>

<div class="layout--page menu--page menu--page-mobile">
  <div class="menu--page-mobile-header">
    <h1 class="button--accent">Digibujo</h1>

    <div class="menu--page-mobile-header-actions">
      <%= render "account_button" %>

      <div class="menu--create-bucket" data-controller="dialog">
        <button
          type="button"
          class="button--primary button--icon-strong button--circle"
          commandfor="home_create"
          command="show-modal"
          aria-label="Create"
        >
          <i
            class="icon"
            style="--icon-mask: var(--icon-plus)"
            aria-hidden="true"
          ></i>
        </button>

        <%= render "shared/dialog", id: "home_create" do %>
          <div class="menu--create-bucket-body">
            <%= render "home_actions" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <%= render "menu/navigation" %>

  <div
    class="menu--search"
    data-controller="combobox search"
    data-combobox-path-value="<%= search_path %>"
    data-search-selection-path-value="<%= search_selection_path %>"
    data-dropdown-sync-closed-status-with-body-value="true"
  >
    <%= render "menu/search" %>
    <%= render "menu/search_palette" %>
  </div>

  <div class="menu--scroll">
    <div class="menu--sections">
      <% @sections.each do |section| %>
        <%= render "home_section", section: section %>
      <% end %>
    </div>
  </div>
</div>
