<turbo-frame id="home_activity">
  <div class="home--activity-list">
    <% if @activities.any? %>
      <% @activities.each do |activity| %>
        <%= render "home/activity", activity: activity %>
      <% end %>
    <% else %>
      <p class="utilities--text-sm" style="color: var(--color-fg-subtle);">No recent activity</p>
    <% end %>
  </div>
  <%= link_to activities_path, class: "button--link home--activity-more", data: { turbo_frame: "_top" }, aria: { label: "Earlier activity" } do %>
  Show more...
<% end %>
</turbo-frame>
