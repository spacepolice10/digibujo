<%# locals: (activity:) %>
<div
  id="<%= dom_id(activity) %>"
  class="home--activity-item"
>

    <p class="home--activity-detail">
      <%= activity_sentence(activity, relative: true) %>
    </p>
</div>
