<%# locals: (tracker:) %>
<% statistics = tracker.statistics %>

<dl id="<%= dom_id(tracker, :statistics) %>" class="layout--surface tracker--statistics" data-elevation="2" data-padding="true">
  <div>
    <dt>Current streak</dt>
    <dd><%= statistics[:streak] %></dd>
  </div>
  <div>
    <dt>Best streak</dt>
    <dd><%= statistics[:best_streak] %></dd>
  </div>
  <div>
    <dt>Total days</dt>
    <dd><%= statistics[:total] %></dd>
  </div>
</dl>
