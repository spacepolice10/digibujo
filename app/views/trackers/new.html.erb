<% content_for(:title) { "New tracker" } %>

<div class="layout--container" data-size="md">
  <div class="layout--grid layout--gapped">
    <div class="button--secondary button--sm layout--mx-auto">
    <%= icon_tag("hash") %>
    <span>New tracker</span>
    </div>
    <p class="layout--mx-auto layout--container typography--paragraph-sm layout--padded typography--balanced typography--center" data-size="sm">Trackers are repeating habits — mark them off each day on your daily log or monthly spread.</p>
  </div>
  <div class="layout--surface">
    <div class="layout--flex layout--justify-between layout--surface-header">
      <%= back_link_to root_path, class: "button--tertiary button--sm" do %>
        <%= icon_tag("arrow-left") %>
        Back
      <% end %>
    </div>
    <div class="layout--padded">
      <%= render "trackers/form",
            tracker: @tracker,
            name_hint: "Give your tracker a name. Examples: Morning run, Meditate, Drink water." %>
    </div>
  </div>
</div>
