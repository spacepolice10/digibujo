<%= link_to future_path(future), class: "bucket--section-list-item" do %>
<%= icon_tag(future.icon, style: "color: #{colour(future.colour)};") %>
  <span class="utilities--line-clamp-1"><%= future.name %></span>
<% end %>
