<div class="bucket--list-item-name">
  <div class="bucket--list-item-marker" data-bucket-colour="<%= bucket.colour %>" aria-hidden="true">
    <%= icon_tag(bucket.icon) %>
  </div>
  <span class="utilities--line-clamp-1"><%= bucket.name %></span>
</div>
