<%= turbo_stream.replace dom_id(@bucket, :pin_button) do %>
  <%= render "buckets/pins_button", bucket: @bucket %>
<% end %>
<%= turbo_stream.update "pinned_buckets_footer",
      partial: "pinned/pinned_buckets_footer",
      locals: { buckets: pinned_buckets } %>
