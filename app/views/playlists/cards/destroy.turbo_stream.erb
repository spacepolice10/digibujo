<%= turbo_stream.remove dom_id(@playlist_card) %>
<%= turbo_stream.replace dom_id(@playlist_card.playlist, :dock) do %>
  <%= render partial: "playlists/dock_stack", locals: { playlist: @playlist_card.playlist } %>
<% end %>
