<%# locals: (activity:) %>
<div
  id="<%= dom_id(activity) %>"
  class="activity--item"
>
  <p class="activity--detail utilities--line-clamp-1">
    <%= activity_sentence(activity, relative: true) %>
  </p>
</div>
