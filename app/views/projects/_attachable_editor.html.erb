<span class="pill<%= project.colour.present? ? " pill--#{project.colour}" : "" %>">
  <% if project.icon.present? %>
    <%= render "shared/entity_icon", record: project %>
  <% end %>
  <span><%= project.name %></span>
</span>
