<%# locals: (project:) %>
<span class="pill--inline-slot">
  <span class="pill pill--inline-sizer" aria-hidden="true">
    <%= render "projects/mark", project: project %>
    <span class="pill--label"><%= project.name %></span>
  </span>
  <span class="pill pill--inline-face">
    <%= render "projects/mark", project: project %>
    <span class="pill--label"><%= project.name %></span>
  </span>
</span>
