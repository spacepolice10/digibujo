<%# locals: (recurrency:) %>
<%= form_with model: recurrency, class: "form form--strong" do |f| %>
  <% if recurrency.errors.any? %>
    <div class="form--errors" role="alert">
      <p><%= pluralize(recurrency.errors.count, "error") %> prevented this recurrency from being saved:</p>
      <ul>
        <% recurrency.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-input", required: true, autofocus: true %>
  </div>

  <%= render "shared/form/colour_picker", form: f, aria_label: "Recurrency colour" %>
  <%= render "shared/form/icon_picker", form: f,
        aria_label: "Recurrency icon",
        hint: "Pick an emoji to spot this recurrency in your daily log." %>

  <fieldset class="form--field">
    <legend>Days</legend>
    <% Date::ABBR_DAYNAMES.each_with_index do |label, wday| %>
      <label class="form--checkbox-label">
        <input type="checkbox"
               name="recurrency[schedule_days][]"
               value="<%= wday %>"
               <% if recurrency.schedule_days.include?(wday) %>checked<% end %>>
        <%= label %>
      </label>
    <% end %>
  </fieldset>

  <div class="form--actions">
    <%= link_to "Cancel", home_path, class: "button--secondary" %>
    <%= f.submit class: "button--primary" %>
  </div>
<% end %>
