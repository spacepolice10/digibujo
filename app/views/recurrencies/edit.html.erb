<% content_for(:title) { "Edit #{@recurrency.name}" } %>

<div class="layout--page form--page">
  <div class="layout--header">
    <div class="layout--header-actions">
      <%= link_to recurrency_path(@recurrency), class: "button--secondary" do %>
        <i class="icon" style="--icon-mask: var(--icon-arrow-left)" aria-hidden="true"></i>
        Back
      <% end %>
    </div>
    <h2>Edit recurrency</h2>
  </div>
  <div class="form--surface">
    <%= render "form", recurrency: @recurrency %>
  </div>
</div>
