<%= turbo_stream.replace dom_id(@recurrency, "#{@dom_key}_#{@date.iso8601}") do %>
  <%= render "recurrencies/toggle",
        recurrency: @recurrency,
        date: @date,
        tracker: @tracker,
        dom_key: @dom_key,
        appearance: @dom_key == "cell" ? :cell : :chip %>
<% end %>
